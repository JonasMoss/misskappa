% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kappa.R
\name{kappa_raw}
\alias{kappa_raw}
\title{Unified Interface for Inter-Rater Agreement Coefficients (Categorical Data)}
\usage{
kappa_raw(
  x,
  method = c("ml", "ipw", "available", "quadratic"),
  weight = c("identity", "unweighted", "linear", "quadratic", "ordinal", "radical",
    "ratio", "circular", "bipolar"),
  values = NULL,
  ...
)
}
\arguments{
\item{x}{A numeric matrix of ratings (subjects-by-raters). Must contain integer
category labels. \code{NA}s are permitted.}

\item{method}{A string specifying the estimation method: "ml", "ipw", "available", or "quadratic".}

\item{weight}{A string specifying the weighting scheme for disagreements.
Supported values are \code{"identity"}, \code{"unweighted"}, \code{"linear"}, \code{"quadratic"},
\code{"ordinal"}, \code{"radical"}, \code{"ratio"}, \code{"circular"}, and \code{"bipolar"}.
This argument is ignored and set to \code{"quadratic"} if \code{method = "quadratic"}.}

\item{values}{An optional numeric vector of values (scores) assigned to each category.
If \code{NULL}, categories are inferred from the data and assigned sequential integer values.}

\item{...}{Additional options passed to the specific estimation method. For
\code{method = "ml"}, this can include \code{em_options = list(tol = ..., max_iter = ...)}
to control the EM algorithm.}
}
\value{
A list containing the point estimates and the variance-covariance matrix.
}
\description{
Computes various weighted agreement coefficients (Conger's Kappa, Fleiss' Kappa,
and Brennan-Prediger) for multiple raters on categorical data.
This is the primary interface for analyzing raw \verb{(subjects x raters)} rating data
where ratings are discrete categories.

For continuous data, see \code{kappa_continuous()}. For count data, see \code{kappa_counts()}.
}
\details{
The \code{method} argument dispatches to different underlying C++ estimation engines.

The \code{"quadratic"} method is a special case that applies quadratic weights based on
numeric scores assigned to the categories.
}
\seealso{
kappa_continuous, kappa_counts
}
