% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kappa_dispatch.R
\name{kappa}
\alias{kappa}
\title{Unified kappa interface}
\usage{
kappa(x, type = c("auto", "raw", "continuous", "counts"), ...)
}
\arguments{
\item{x}{A matrix/data frame of ratings. Interpretation depends on \code{type}.}

\item{type}{Which data type to use. If \code{"auto"}, dispatch uses a simple heuristic:
\itemize{
\item If any non-integer values are present, uses \code{"continuous"}.
\item Otherwise uses \code{"raw"}, unless the matrix is non-missing, nonnegative integer
and either \code{r} is supplied (via \code{...}) or all row sums are equal, in which case
it uses \code{"counts"}.
}}

\item{...}{Passed through to the underlying \code{kappa_*()} function.}
}
\value{
An object of class \code{misskappa_estimate}.
}
\description{
Convenience wrapper that dispatches to one of \code{kappa_raw()}, \code{kappa_continuous()},
or \code{kappa_counts()} depending on \code{type}.
}
\examples{
x_raw <- matrix(c(1, 1, NA,
                  2, 2, 2,
                  1, 2, 1),
                nrow = 3, byrow = TRUE)
kappa(x_raw, type = "raw", method = "available")
kappa(x_raw)  # auto dispatch

x_cont <- matrix(c(0.1, 0.1, NA,
                   0.0, 0.2, 0.1,
                   0.9, 1.0, 0.8),
                 nrow = 3, byrow = TRUE)
kappa(x_cont)  # auto -> continuous

x_counts <- matrix(c(2, 1, 0,
                     1, 1, 1,
                     0, 3, 0),
                   nrow = 3, byrow = TRUE)
kappa(x_counts, type = "counts", method = "available", r = 3)
}
