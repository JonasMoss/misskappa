r <- length(s)
n <- 100
true_dist <- c(0.1, 0.2, 0.3, 0.3, 0.1)
pi_tensor <- array(0, dim = rep(2, r))
pi_tensor[1,1,1] <- 0.40
pi_tensor[1,1,2] <- 0.10
pi_tensor[1,2,1] <- 0.15
pi_tensor[2,1,1] <- 0.05
pi_tensor[1,2,2] <- 0.10
pi_tensor[2,1,2] <- 0.10
pi_tensor[2,2,1] <- 0.05
pi_tensor[2,2,2] <- 0.05
introduce_missingness <- function(data, mechanism_def) {
n_subjects <- nrow(data)
n_raters <- ncol(data)
if (is.array(mechanism_def) && length(dim(mechanism_def)) == n_raters) {
if (any(dim(mechanism_def) != 2)) stop("Tensor dimensions must be 2.")
if (abs(sum(mechanism_def) - 1) > 1e-9) stop("Tensor probabilities must sum to 1.")
all_patterns <- expand.grid(replicate(n_raters, c(1, 0), simplify = FALSE))
sampled_indices <- sample.int(2^n_raters, size = n_subjects, replace = TRUE, prob = c(mechanism_def))
chosen_patterns <- all_patterns[sampled_indices, , drop = FALSE]
na_mask <- (chosen_patterns == 0)
data[na_mask] <- NA
} else if (is.vector(mechanism_def) && !is.list(mechanism_def)) {
for (j in 1:n_raters) {
na_mask <- sample(c(TRUE, FALSE), size = n_subjects, replace = TRUE, prob = c(mechanism_def[j], 1 - mechanism_def[j]))
data[na_mask, j] <- NA
}
} else {
stop("Invalid 'mechanism_def' format.")
}
return(data)
}
results <- replicate(100, {
x <- simulate_jsm(n = n, s = s, model = "fleiss", true_dist = true_dist)
y <- introduce_missingness(x, pi_tensor)
c(kappa_raw(y, method = "ml")$estimates["Conger"], kappa_raw(x, method = "ipw")$estimates["Conger"])
})
results
kappa <- simulate_jsm(0, s, "fliess", true_dist = true_dist)
kappa <- simulate_jsm(n = 0, s = s, model = "fliess", true_dist = true_dist)
kappa <- simulate_jsm(n = 1, s = s, model = "fliess", true_dist = true_dist)
kappa <- simulate_jsm(n = 1, s = s, model = "fleiss", true_dist = true_dist)
kappa <- simulate_jsm(n = 0, s = s, model = "fleiss", true_dist = true_dist)
kappa <- simulate_jsm(n = 1, s = s, model = "fleiss", true_dist = true_dist)
kappa
kappa <- attr(simulate_jsm(n = 1, s = s, model = "fleiss", true_dist = true_dist), "kappa")
kappa
n <- 100
s <- c(0.9, 0.8, 0.7)
r <- length(s)
n <- 100
true_dist <- c(0.1, 0.2, 0.3, 0.3, 0.1)
pi_tensor <- array(0, dim = rep(2, r))
pi_tensor[1,1,1] <- 0.40
pi_tensor[1,1,2] <- 0.10
pi_tensor[1,2,1] <- 0.15
pi_tensor[2,1,1] <- 0.05
pi_tensor[1,2,2] <- 0.10
pi_tensor[2,1,2] <- 0.10
pi_tensor[2,2,1] <- 0.05
pi_tensor[2,2,2] <- 0.05
introduce_missingness <- function(data, mechanism_def) {
n_subjects <- nrow(data)
n_raters <- ncol(data)
if (is.array(mechanism_def) && length(dim(mechanism_def)) == n_raters) {
if (any(dim(mechanism_def) != 2)) stop("Tensor dimensions must be 2.")
if (abs(sum(mechanism_def) - 1) > 1e-9) stop("Tensor probabilities must sum to 1.")
all_patterns <- expand.grid(replicate(n_raters, c(1, 0), simplify = FALSE))
sampled_indices <- sample.int(2^n_raters, size = n_subjects, replace = TRUE, prob = c(mechanism_def))
chosen_patterns <- all_patterns[sampled_indices, , drop = FALSE]
na_mask <- (chosen_patterns == 0)
data[na_mask] <- NA
} else if (is.vector(mechanism_def) && !is.list(mechanism_def)) {
for (j in 1:n_raters) {
na_mask <- sample(c(TRUE, FALSE), size = n_subjects, replace = TRUE, prob = c(mechanism_def[j], 1 - mechanism_def[j]))
data[na_mask, j] <- NA
}
} else {
stop("Invalid 'mechanism_def' format.")
}
return(data)
}
kappa <- attr(simulate_jsm(n = 1, s = s, model = "fleiss", true_dist = true_dist), "kappa")
results <- replicate(100, {
x <- simulate_jsm(n = n, s = s, model = "fleiss", true_dist = true_dist)
y <- introduce_missingness(x, pi_tensor)
c(kappa_raw(y, method = "ml")$estimates["Conger"], kappa_raw(x, method = "ipw")$estimates["Conger"])
})
results
n <- 100
s <- c(0.9, 0.8, 0.7)
r <- length(s)
n <- 100
true_dist <- c(0.1, 0.2, 0.3, 0.3, 0.1)
pi_tensor <- array(0, dim = rep(2, r))
pi_tensor[1,1,1] <- 0.40
pi_tensor[1,1,2] <- 0.10
pi_tensor[1,2,1] <- 0.15
pi_tensor[2,1,1] <- 0.05
pi_tensor[1,2,2] <- 0.10
pi_tensor[2,1,2] <- 0.10
pi_tensor[2,2,1] <- 0.05
pi_tensor[2,2,2] <- 0.05
introduce_missingness <- function(data, mechanism_def) {
n_subjects <- nrow(data)
n_raters <- ncol(data)
if (is.array(mechanism_def) && length(dim(mechanism_def)) == n_raters) {
if (any(dim(mechanism_def) != 2)) stop("Tensor dimensions must be 2.")
if (abs(sum(mechanism_def) - 1) > 1e-9) stop("Tensor probabilities must sum to 1.")
all_patterns <- expand.grid(replicate(n_raters, c(1, 0), simplify = FALSE))
sampled_indices <- sample.int(2^n_raters, size = n_subjects, replace = TRUE, prob = c(mechanism_def))
chosen_patterns <- all_patterns[sampled_indices, , drop = FALSE]
na_mask <- (chosen_patterns == 0)
data[na_mask] <- NA
} else if (is.vector(mechanism_def) && !is.list(mechanism_def)) {
for (j in 1:n_raters) {
na_mask <- sample(c(TRUE, FALSE), size = n_subjects, replace = TRUE, prob = c(mechanism_def[j], 1 - mechanism_def[j]))
data[na_mask, j] <- NA
}
} else {
stop("Invalid 'mechanism_def' format.")
}
return(data)
}
kappa <- attr(simulate_jsm(n = 1, s = s, model = "fleiss", true_dist = true_dist), "kappa")
results <- replicate(100, {
x <- simulate_jsm(n = n, s = s, model = "fleiss", true_dist = true_dist)
y <- introduce_missingness(x, pi_tensor)
c(kappa_raw(y, method = "ml")$estimates["Conger"], kappa_raw(x, method = "ipw")$estimates["Conger"])
})
colMeans((results - kappa)^2)
results
n <- 100
s <- c(0.9, 0.8, 0.7)
r <- length(s)
n <- 100
true_dist <- c(0.1, 0.2, 0.3, 0.3, 0.1)
pi_tensor <- array(0, dim = rep(2, r))
pi_tensor[1,1,1] <- 0.40
pi_tensor[1,1,2] <- 0.10
pi_tensor[1,2,1] <- 0.15
pi_tensor[2,1,1] <- 0.05
pi_tensor[1,2,2] <- 0.10
pi_tensor[2,1,2] <- 0.10
pi_tensor[2,2,1] <- 0.05
pi_tensor[2,2,2] <- 0.05
introduce_missingness <- function(data, mechanism_def) {
n_subjects <- nrow(data)
n_raters <- ncol(data)
if (is.array(mechanism_def) && length(dim(mechanism_def)) == n_raters) {
if (any(dim(mechanism_def) != 2)) stop("Tensor dimensions must be 2.")
if (abs(sum(mechanism_def) - 1) > 1e-9) stop("Tensor probabilities must sum to 1.")
all_patterns <- expand.grid(replicate(n_raters, c(1, 0), simplify = FALSE))
sampled_indices <- sample.int(2^n_raters, size = n_subjects, replace = TRUE, prob = c(mechanism_def))
chosen_patterns <- all_patterns[sampled_indices, , drop = FALSE]
na_mask <- (chosen_patterns == 0)
data[na_mask] <- NA
} else if (is.vector(mechanism_def) && !is.list(mechanism_def)) {
for (j in 1:n_raters) {
na_mask <- sample(c(TRUE, FALSE), size = n_subjects, replace = TRUE, prob = c(mechanism_def[j], 1 - mechanism_def[j]))
data[na_mask, j] <- NA
}
} else {
stop("Invalid 'mechanism_def' format.")
}
return(data)
}
kappa <- attr(simulate_jsm(n = 1, s = s, model = "fleiss", true_dist = true_dist), "kappa")
results <- replicate(100, {
x <- simulate_jsm(n = n, s = s, model = "fleiss", true_dist = true_dist)
y <- introduce_missingness(x, pi_tensor)
c(kappa_raw(y, method = "ml")$estimates["Conger"], kappa_raw(x, method = "ipw")$estimates["Conger"])
})
rowMeans((results - kappa)^2)
n <- 100
s <- c(0.9, 0.8, 0.7)
r <- length(s)
n <- 100
true_dist <- c(0.1, 0.2, 0.3, 0.3, 0.1)
pi_tensor <- array(0, dim = rep(2, r))
pi_tensor[1,1,1] <- 0.40
pi_tensor[1,1,2] <- 0.10
pi_tensor[1,2,1] <- 0.15
pi_tensor[2,1,1] <- 0.05
pi_tensor[1,2,2] <- 0.10
pi_tensor[2,1,2] <- 0.10
pi_tensor[2,2,1] <- 0.05
pi_tensor[2,2,2] <- 0.05
introduce_missingness <- function(data, mechanism_def) {
n_subjects <- nrow(data)
n_raters <- ncol(data)
if (is.array(mechanism_def) && length(dim(mechanism_def)) == n_raters) {
if (any(dim(mechanism_def) != 2)) stop("Tensor dimensions must be 2.")
if (abs(sum(mechanism_def) - 1) > 1e-9) stop("Tensor probabilities must sum to 1.")
all_patterns <- expand.grid(replicate(n_raters, c(1, 0), simplify = FALSE))
sampled_indices <- sample.int(2^n_raters, size = n_subjects, replace = TRUE, prob = c(mechanism_def))
chosen_patterns <- all_patterns[sampled_indices, , drop = FALSE]
na_mask <- (chosen_patterns == 0)
data[na_mask] <- NA
} else if (is.vector(mechanism_def) && !is.list(mechanism_def)) {
for (j in 1:n_raters) {
na_mask <- sample(c(TRUE, FALSE), size = n_subjects, replace = TRUE, prob = c(mechanism_def[j], 1 - mechanism_def[j]))
data[na_mask, j] <- NA
}
} else {
stop("Invalid 'mechanism_def' format.")
}
return(data)
}
kappa <- attr(simulate_jsm(n = 1, s = s, model = "fleiss", true_dist = true_dist), "kappa")
results <- replicate(1000, {
x <- simulate_jsm(n = n, s = s, model = "fleiss", true_dist = true_dist)
y <- introduce_missingness(x, pi_tensor)
c(kappa_raw(y, method = "ml")$estimates["Conger"], kappa_raw(x, method = "ipw")$estimates["Conger"])
})
rowMeans((results - kappa)^2)
dim(pi_tensor)
n <- 100
s <- c(0.9, 0.8, 0.7)
r <- length(s)
n <- 100
true_dist <- c(0.1, 0.2, 0.3, 0.3, 0.1)
pi_tensor <- array(0, dim = rep(2, r))
pi_tensor[1,1,1] <- 0.40
pi_tensor[1,1,2] <- 0.10
pi_tensor[1,2,1] <- 0.15
pi_tensor[2,1,1] <- 0.05
pi_tensor[1,2,2] <- 0.10
pi_tensor[2,1,2] <- 0.10
pi_tensor[2,2,1] <- 0.05
pi_tensor[2,2,2] <- 0.05
pi_tensor <- array(0, dim = rep(2, r))
pi_tensor[1,1,1] <- 0.40
pi_tensor[1,1,2] <- 0.10
pi_tensor[1,2,1] <- 0.15
pi_tensor[2,1,1] <- 0.05
pi_tensor[1,2,2] <- 0.10
pi_tensor[2,1,2] <- 0.10
pi_tensor[2,2,1] <- 0.05
pi_tensor[2,2,2] <- 0.05
introduce_missingness <- function(data, mechanism_def) {
n_subjects <- nrow(data)
n_raters <- ncol(data)
if (is.array(mechanism_def) && length(dim(mechanism_def)) == n_raters) {
if (any(dim(mechanism_def) != 2)) stop("Tensor dimensions must be 2.")
if (abs(sum(mechanism_def) - 1) > 1e-9) stop("Tensor probabilities must sum to 1.")
all_patterns <- expand.grid(replicate(n_raters, c(1, 0), simplify = FALSE))
sampled_indices <- sample.int(2^n_raters, size = n_subjects, replace = TRUE, prob = c(mechanism_def))
chosen_patterns <- all_patterns[sampled_indices, , drop = FALSE]
na_mask <- (chosen_patterns == 0)
data[na_mask] <- NA
} else if (is.vector(mechanism_def) && !is.list(mechanism_def)) {
for (j in 1:n_raters) {
na_mask <- sample(c(TRUE, FALSE), size = n_subjects, replace = TRUE, prob = c(mechanism_def[j], 1 - mechanism_def[j]))
data[na_mask, j] <- NA
}
} else {
stop("Invalid 'mechanism_def' format.")
}
return(data)
}
kappa <- attr(simulate_jsm(n = 1, s = s, model = "fleiss", true_dist = true_dist), "kappa")
results <- replicate(1000, {
x <- simulate_jsm(n = n, s = s, model = "fleiss", true_dist = true_dist)
y <- introduce_missingness(x, pi_tensor)
c(kappa_raw(y, method = "ml")$estimates["Conger"], kappa_raw(x, method = "ipw")$estimates["Conger"])
})
rowMeans((results - kappa)^2)
s <- c(0.9, 0.8, 0.7)
r <- length(s)
n <- 200
true_dist <- c(0.1, 0.2, 0.3, 0.3, 0.1)
pi_tensor <- array(0, dim = rep(2, r))
pi_tensor[1,1,1] <- 0.40
pi_tensor[1,1,2] <- 0.10
pi_tensor[1,2,1] <- 0.15
pi_tensor[2,1,1] <- 0.05
pi_tensor[1,2,2] <- 0.10
pi_tensor[2,1,2] <- 0.10
pi_tensor[2,2,1] <- 0.05
pi_tensor[2,2,2] <- 0.05
pi_tensor <- array(0, dim = rep(2, r))
pi_tensor[1,1,1] <- 0.40
pi_tensor[1,1,2] <- 0.10
pi_tensor[1,2,1] <- 0.15
pi_tensor[2,1,1] <- 0.05
pi_tensor[1,2,2] <- 0.10
pi_tensor[2,1,2] <- 0.10
pi_tensor[2,2,1] <- 0.05
pi_tensor[2,2,2] <- 0.05
introduce_missingness <- function(data, mechanism_def) {
n_subjects <- nrow(data)
n_raters <- ncol(data)
if (is.array(mechanism_def) && length(dim(mechanism_def)) == n_raters) {
if (any(dim(mechanism_def) != 2)) stop("Tensor dimensions must be 2.")
if (abs(sum(mechanism_def) - 1) > 1e-9) stop("Tensor probabilities must sum to 1.")
all_patterns <- expand.grid(replicate(n_raters, c(1, 0), simplify = FALSE))
sampled_indices <- sample.int(2^n_raters, size = n_subjects, replace = TRUE, prob = c(mechanism_def))
chosen_patterns <- all_patterns[sampled_indices, , drop = FALSE]
na_mask <- (chosen_patterns == 0)
data[na_mask] <- NA
} else if (is.vector(mechanism_def) && !is.list(mechanism_def)) {
for (j in 1:n_raters) {
na_mask <- sample(c(TRUE, FALSE), size = n_subjects, replace = TRUE, prob = c(mechanism_def[j], 1 - mechanism_def[j]))
data[na_mask, j] <- NA
}
} else {
stop("Invalid 'mechanism_def' format.")
}
return(data)
}
kappa <- attr(simulate_jsm(n = 1, s = s, model = "fleiss", true_dist = true_dist), "kappa")
results <- replicate(1000, {
x <- simulate_jsm(n = n, s = s, model = "fleiss", true_dist = true_dist)
y <- introduce_missingness(x, pi_tensor)
c(kappa_raw(y, method = "ml")$estimates["Conger"], kappa_raw(x, method = "ipw")$estimates["Conger"])
})
rowMeans((results - kappa)^2) * n
s <- c(0.9, 0.8, 0.7)
r <- length(s)
n <- 200
true_dist <- c(0.1, 0.2, 0.3, 0.3, 0.1)
pi_tensor <- array(0, dim = rep(2, r))
pi_tensor[1,1,1] <- 0.40
pi_tensor[1,1,2] <- 0.10
pi_tensor[1,2,1] <- 0.15
pi_tensor[2,1,1] <- 0.05
pi_tensor[1,2,2] <- 0.10
pi_tensor[2,1,2] <- 0.10
pi_tensor[2,2,1] <- 0.05
pi_tensor[2,2,2] <- 0.05
pi_tensor <- array(0, dim = rep(2, r))
pi_tensor[1,1,1] <- 0.40
pi_tensor[1,1,2] <- 0.10
pi_tensor[1,2,1] <- 0.15
pi_tensor[2,1,1] <- 0.05
pi_tensor[1,2,2] <- 0.10
pi_tensor[2,1,2] <- 0.10
pi_tensor[2,2,1] <- 0.05
pi_tensor[2,2,2] <- 0.05
introduce_missingness <- function(data, mechanism_def) {
n_subjects <- nrow(data)
n_raters <- ncol(data)
if (is.array(mechanism_def) && length(dim(mechanism_def)) == n_raters) {
if (any(dim(mechanism_def) != 2)) stop("Tensor dimensions must be 2.")
if (abs(sum(mechanism_def) - 1) > 1e-9) stop("Tensor probabilities must sum to 1.")
all_patterns <- expand.grid(replicate(n_raters, c(1, 0), simplify = FALSE))
sampled_indices <- sample.int(2^n_raters, size = n_subjects, replace = TRUE, prob = c(mechanism_def))
chosen_patterns <- all_patterns[sampled_indices, , drop = FALSE]
na_mask <- (chosen_patterns == 0)
data[na_mask] <- NA
} else if (is.vector(mechanism_def) && !is.list(mechanism_def)) {
for (j in 1:n_raters) {
na_mask <- sample(c(TRUE, FALSE), size = n_subjects, replace = TRUE, prob = c(mechanism_def[j], 1 - mechanism_def[j]))
data[na_mask, j] <- NA
}
} else {
stop("Invalid 'mechanism_def' format.")
}
return(data)
}
kappa <- attr(simulate_jsm(n = 1, s = s, model = "fleiss", true_dist = true_dist), "kappa")
results <- replicate(1000, {
x <- simulate_jsm(n = n, s = s, model = "fleiss", true_dist = true_dist)
y <- introduce_missingness(x, pi_tensor)
c(kappa_raw(y, method = "ml")$estimates["Conger"], kappa_raw(y, method = "ipw")$estimates["Conger"])
})
rowMeans((results - kappa)^2) * n
s <- c(0.9, 0.8, 0.7)
r <- length(s)
n <- 1000
true_dist <- c(0.1, 0.2, 0.3, 0.3, 0.1)
pi_tensor <- array(0, dim = rep(2, r))
pi_tensor[1,1,1] <- 0.40
pi_tensor[1,1,2] <- 0.10
pi_tensor[1,2,1] <- 0.15
pi_tensor[2,1,1] <- 0.05
pi_tensor[1,2,2] <- 0.10
pi_tensor[2,1,2] <- 0.10
pi_tensor[2,2,1] <- 0.05
pi_tensor[2,2,2] <- 0.05
pi_tensor <- array(0, dim = rep(2, r))
pi_tensor[1,1,1] <- 0.40
pi_tensor[1,1,2] <- 0.10
pi_tensor[1,2,1] <- 0.15
pi_tensor[2,1,1] <- 0.05
pi_tensor[1,2,2] <- 0.10
pi_tensor[2,1,2] <- 0.10
pi_tensor[2,2,1] <- 0.05
pi_tensor[2,2,2] <- 0.05
introduce_missingness <- function(data, mechanism_def) {
n_subjects <- nrow(data)
n_raters <- ncol(data)
if (is.array(mechanism_def) && length(dim(mechanism_def)) == n_raters) {
if (any(dim(mechanism_def) != 2)) stop("Tensor dimensions must be 2.")
if (abs(sum(mechanism_def) - 1) > 1e-9) stop("Tensor probabilities must sum to 1.")
all_patterns <- expand.grid(replicate(n_raters, c(1, 0), simplify = FALSE))
sampled_indices <- sample.int(2^n_raters, size = n_subjects, replace = TRUE, prob = c(mechanism_def))
chosen_patterns <- all_patterns[sampled_indices, , drop = FALSE]
na_mask <- (chosen_patterns == 0)
data[na_mask] <- NA
} else if (is.vector(mechanism_def) && !is.list(mechanism_def)) {
for (j in 1:n_raters) {
na_mask <- sample(c(TRUE, FALSE), size = n_subjects, replace = TRUE, prob = c(mechanism_def[j], 1 - mechanism_def[j]))
data[na_mask, j] <- NA
}
} else {
stop("Invalid 'mechanism_def' format.")
}
return(data)
}
kappa <- attr(simulate_jsm(n = 1, s = s, model = "fleiss", true_dist = true_dist), "kappa")
results <- replicate(1000, {
x <- simulate_jsm(n = n, s = s, model = "fleiss", true_dist = true_dist)
y <- introduce_missingness(x, pi_tensor)
c(kappa_raw(y, method = "ml")$estimates["Conger"], kappa_raw(y, method = "ipw")$estimates["Conger"])
})
rowMeans((results - kappa)^2) * n
s <- c(0.9, 0.8, 0.7)
r <- length(s)
n <- 1000
true_dist <- c(0.1, 0.2, 0.3, 0.3, 0.1)
pi_tensor <- array(0, dim = rep(2, r))
pi_tensor[1,1,1] <- 0.40
pi_tensor[1,1,2] <- 0.10
pi_tensor[1,2,1] <- 0.15
pi_tensor[2,1,1] <- 0.05
pi_tensor[1,2,2] <- 0.10
pi_tensor[2,1,2] <- 0.10
pi_tensor[2,2,1] <- 0.05
pi_tensor[2,2,2] <- 0.05
pi_tensor <- array(0, dim = rep(2, r))
pi_tensor[1,1,1] <- 0.40
pi_tensor[1,1,2] <- 0.10
pi_tensor[1,2,1] <- 0.15
pi_tensor[2,1,1] <- 0.05
pi_tensor[1,2,2] <- 0.10
pi_tensor[2,1,2] <- 0.10
pi_tensor[2,2,1] <- 0.05
pi_tensor[2,2,2] <- 0.05
introduce_missingness <- function(data, mechanism_def) {
n_subjects <- nrow(data)
n_raters <- ncol(data)
if (is.array(mechanism_def) && length(dim(mechanism_def)) == n_raters) {
if (any(dim(mechanism_def) != 2)) stop("Tensor dimensions must be 2.")
if (abs(sum(mechanism_def) - 1) > 1e-9) stop("Tensor probabilities must sum to 1.")
all_patterns <- expand.grid(replicate(n_raters, c(1, 0), simplify = FALSE))
sampled_indices <- sample.int(2^n_raters, size = n_subjects, replace = TRUE, prob = c(mechanism_def))
chosen_patterns <- all_patterns[sampled_indices, , drop = FALSE]
na_mask <- (chosen_patterns == 0)
data[na_mask] <- NA
} else if (is.vector(mechanism_def) && !is.list(mechanism_def)) {
for (j in 1:n_raters) {
na_mask <- sample(c(TRUE, FALSE), size = n_subjects, replace = TRUE, prob = c(mechanism_def[j], 1 - mechanism_def[j]))
data[na_mask, j] <- NA
}
} else {
stop("Invalid 'mechanism_def' format.")
}
return(data)
}
kappa <- attr(simulate_jsm(n = 1, s = s, model = "fleiss", true_dist = true_dist), "kappa")
results <- replicate(10000, {
x <- simulate_jsm(n = n, s = s, model = "fleiss", true_dist = true_dist)
y <- introduce_missingness(x, pi_tensor)
c(kappa_raw(y, method = "ml")$estimates["Conger"],
kappa_raw(y, method = "ipw")$estimates["Conger"])
})
rowMeans((results - kappa)^2) * n
s <- c(0.9, 0.8, 0.7)
r <- length(s)
n <- 1000
true_dist <- c(0.1, 0.2, 0.3, 0.3, 0.1)
pi_tensor <- array(0, dim = rep(2, r))
pi_tensor[1,1,1] <- 0.40
pi_tensor[1,1,2] <- 0.10
pi_tensor[1,2,1] <- 0.15
pi_tensor[2,1,1] <- 0.05
pi_tensor[1,2,2] <- 0.10
pi_tensor[2,1,2] <- 0.10
pi_tensor[2,2,1] <- 0.10
pi_tensor[2,2,2] <- 0.00
introduce_missingness <- function(data, mechanism_def) {
n_subjects <- nrow(data)
n_raters <- ncol(data)
if (is.array(mechanism_def) && length(dim(mechanism_def)) == n_raters) {
if (any(dim(mechanism_def) != 2)) stop("Tensor dimensions must be 2.")
if (abs(sum(mechanism_def) - 1) > 1e-9) stop("Tensor probabilities must sum to 1.")
all_patterns <- expand.grid(replicate(n_raters, c(1, 0), simplify = FALSE))
sampled_indices <- sample.int(2^n_raters, size = n_subjects, replace = TRUE, prob = c(mechanism_def))
chosen_patterns <- all_patterns[sampled_indices, , drop = FALSE]
na_mask <- (chosen_patterns == 0)
data[na_mask] <- NA
} else if (is.vector(mechanism_def) && !is.list(mechanism_def)) {
for (j in 1:n_raters) {
na_mask <- sample(c(TRUE, FALSE), size = n_subjects, replace = TRUE, prob = c(mechanism_def[j], 1 - mechanism_def[j]))
data[na_mask, j] <- NA
}
} else {
stop("Invalid 'mechanism_def' format.")
}
return(data)
}
kappa <- attr(simulate_jsm(n = 1, s = s, model = "fleiss", true_dist = true_dist), "kappa")
results <- replicate(10000, {
x <- simulate_jsm(n = n, s = s, model = "fleiss", true_dist = true_dist)
y <- introduce_missingness(x, pi_tensor)
c(kappa_raw(y, method = "ml")$estimates["Conger"],
kappa_raw(y, method = "ipw")$estimates["Conger"])
})
rowMeans((results - kappa)^2) * n
