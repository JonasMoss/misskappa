# Analysis scripts

This folder contains paper-facing analysis scripts that use the `misskappa` package and write LaTeX-ready outputs (typically into `notes/`).

Run scripts from the repository root, e.g.:

`Rscript code/analysis/fleiss1971_countform_irrCAC_report.R`
