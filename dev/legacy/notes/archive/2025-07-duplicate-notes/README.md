# Duplicate Notes Snapshot (2025-07)

This directory is a moved copy of the old `notes/notes/` folder (kept as-is for now).

- `conditional_expectation.lyx`, `horvitz-beta.lyx`, `keep2.lyx`: older scratch/derivation notes.
- `misskappa2.lyx`, `pairwise2.lyx`: older draft variants.
- `package.lyx`, `rust_concept.lyx`: implementation notes.
- `semiparam2.lyx`, `semiparametric_notes.lyx`, `vdv_notes.lyx`, `wasserman.lyx`: reference notes.
