% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quadagree-package.R
\name{kappa}
\alias{kappa}
\alias{kappa_raw}
\alias{kappa_aggr}
\title{Confidence Intervals for Quadratic Kappa Coefficients}
\usage{
kappa_raw(
  x,
  transform = c("none", "fisher", "log", "arcsin"),
  conf_level = 0.95,
  alternative = c("two.sided", "greater", "less"),
  bootstrap = FALSE,
  n_reps = 1000,
  seed = NULL
)

kappa_aggr(
  x,
  values = NULL,
  r = NULL,
  transform = c("none", "fisher", "log", "arcsin"),
  conf_level = 0.95,
  alternative = c("two.sided", "greater", "less"),
  bootstrap = FALSE,
  n_reps = 1000,
  seed = NULL
)
}
\arguments{
\item{x}{A matrix or object coercible to a matrix. Each row is a subject, each column a rater (\code{kappa_raw}) or rating count (\code{kappa_aggr}).}

\item{transform}{A variance-stabilizing transform to apply before CI construction. One of \code{"none"} (default), \code{"fisher"}, \code{"log"}, \code{"arcsin"}.}

\item{conf_level}{Confidence level for the CI. Default is \code{0.95}.}

\item{alternative}{Alternative hypothesis for CI. One of \code{"two.sided"} (default), \code{"greater"}, \code{"less"}.}

\item{bootstrap}{Logical. If \code{TRUE}, use the studentized bootstrap. Default is \code{FALSE}.}

\item{n_reps}{Number of bootstrap replicates (ignored if \code{bootstrap = FALSE}). Default is \code{1000}.}

\item{seed}{Optional random seed passed to bootstrap replicates.}

\item{values}{A numeric vector assigning a numeric value to each category (only used in \code{kappa_aggr}). Defaults to \code{1:ncol(x)}.}

\item{r}{Number of raters for \code{kappa_aggr}. Defaults to the maximum number of ratings per item.  Defaults to the maximal number of raters encountered in a row.}
}
\value{
An object of class \code{"quadagree_raw"} or \code{"quadagree_aggr"} with components for Fleiss' and Conger's kappa, including confidence intervals, standard errors, and relevant attributes.
}
\description{
Estimates the quadratic Fleiss' kappa, Conger's (Cohen's) kappa, and the
Brennan--Prediger coefficient for data in either long or aggregated form,
along with confidence intervals based on the delta method or
the studentized bootstrap.
}
\section{Methods}{

\itemize{
\item \code{kappa_raw()} applies to \emph{long form} data where each row is a rater's scores. Supports data missing completely at random.
\item \code{kappa_aggr()} applies to \emph{aggregated form} data where each row contains counts per category. Supports data missing completely at random.
}

Transformations are used to improve confidence interval coverage. Supported options include Fisher's \emph{z}-transform, logit, and arcsin.

The bootstrap procedure is a \emph{studentized bootstrap} with variance estimated per replicate, ensuring second-order correctness (Efron, 1987).
}

\examples{
kappa_raw(dat.klein2018, transform = "fisher")
kappa_aggr(dat.fleiss1971)
}
\references{
Efron, B. (1987). Better Bootstrap Confidence Intervals. \emph{JASA}, 82(397), 171–185.

van Praag, B. M. S., Dijkstra, T. K., & van Velzen, J. (1985). Least-squares theory based on general distributional assumptions with an application to the incomplete observations problem. \emph{Psychometrika}, 50(1), 25–36.

Magnus, J. R., & Neudecker, H. (2019). \emph{Matrix Differential Calculus with Applications in Statistics and Econometrics}. Wiley.

Moss, J. & van Oest, R. (work in progress). Inference for quadratically weighted multi-rater kappas with missing raters.
}
