% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/misc.R
\name{simulate_jsm}
\alias{simulate_jsm}
\title{Simulate ratings from a skill-difficulty agreement model}
\usage{
simulate_jsm(
  n,
  s,
  model = c("general", "cohen-fleiss", "fleiss", "bp", "tu"),
  true_dist = NULL,
  guessing_dist = NULL
)
}
\arguments{
\item{n}{Number of items to simulate.}

\item{s}{Numeric vector of rater skill levels between 0 and 1.}

\item{model}{One of \code{"general"}, \code{"cohen-fleiss"}, \code{"fleiss"}, \code{"bp"}, or \code{"tu"}.}

\item{true_dist}{Optional numeric vector of probabilities for the true class distribution.}

\item{guessing_dist}{Optional guessing distribution: either a vector (shared across raters) or a matrix (rater-specific).}
}
\value{
A matrix of simulated ratings (n rows, J columns) with attributes:
\itemize{
\item \code{"n"}: number of items
\item \code{"s"}: skill vector
\item \code{"true_dist"}: normalized true class distribution
\item \code{"guessing_dist"}: normalized guessing distribution
\item \code{"kappa"}: true latent agreement value under the model
}
}
\description{
This function simulates a matrix of categorical ratings under a class of skill-based guessing models
described in Moss (2023). Each rater has a skill level between 0 and 1. For each item, the true class
is drawn from a true distribution, and raters either guess or respond correctly depending on their skill.

Several models are supported:
\itemize{
\item \code{"general"}: Fully user-specified \code{true_dist} and \code{guessing_dist}
\item \code{"cohen-fleiss"}: True labels are marginally distributed like the weighted average of guessing distributions
\item \code{"fleiss"}: Guessing distribution equals \code{true_dist} (classic Fleiss' kappa assumption)
\item \code{"bp"}: Guessing is uniform, with user-specified \code{true_dist}
\item \code{"tu"}: True distribution is uniform, with user-specified \code{guessing_dist}
}

Agreement is governed by the skill vector \code{s}, and the true agreement is returned as an attribute
(\code{"kappa"}) using the latent agreement definition from Moss (2023).
}
\references{
Moss, J. (2023). Measuring Agreement Using Guessing Models and Knowledge Coefficients.
\emph{Psychometrika}, \doi{10.1007/s11336-023-09887-2} https://arxiv.org/abs/2309.03613
}
\keyword{internal}
