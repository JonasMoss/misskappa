% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kappa.R
\name{kappa_counts}
\alias{kappa_counts}
\title{Unified Interface for Inter-Rater Agreement (Counts Data)}
\usage{
kappa_counts(
  x,
  method = c("ml", "quadratic", "available"),
  weight = c("identity", "unweighted", "linear", "quadratic", "ordinal", "radical",
    "ratio", "circular", "bipolar"),
  values = NULL,
  r = NULL,
  ...
)
}
\arguments{
\item{x}{A numeric matrix of counts (subjects x categories).}

\item{method}{A string specifying the estimation method: "ml", "quadratic", or "available".}

\item{weight}{A string specifying the weighting scheme. Ignored if \code{method = "quadratic"}.}

\item{values}{An optional numeric vector of category values.}

\item{r}{Optional number of raters in the data set. If NULL, it is inferred from
the maximum row sum of \code{x}.}

\item{...}{Additional options, e.g., \code{em_options = list(...)} for method "ml".}
}
\value{
An object of class \code{"misskappa_estimate"} containing the point estimates and
the variance-covariance matrix.
}
\description{
Computes Fleiss' Kappa and the Brennan-Prediger coefficient from count data.
For Conger's Kappa, data must be in the raw (subjects x raters) format.

The counts format aggregates away per-rater missingness patterns, so methods like \code{"ipw"}
and \code{"gwet"} are not currently supported for \code{kappa_counts()}.
}
\examples{
x <- matrix(c(2, 1, 0,
              1, 1, 1,
              0, 3, 0),
            nrow = 3, byrow = TRUE)
fit <- kappa_counts(x, method = "available", weight = "identity", r = 3)
fit
coef(fit)
vcov(fit)
}
\seealso{
kappa_raw, kappa_continuous
}
